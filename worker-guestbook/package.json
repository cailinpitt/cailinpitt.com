{
  "name": "cailinpitt-guestbook-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "tsc --noEmit",
    "schema:local": "wrangler d1 execute cailinpitt-guestbook --local --file=schema.sql",
    "schema:remote": "wrangler d1 execute cailinpitt-guestbook --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
